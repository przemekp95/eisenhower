[pytest]
addopts = --cov=app --cov=main --cov-report=term-missing --cov-fail-under=75
testpaths = tests
markers =
    unit: fast unit-level tests
    contract: consumer contract tests that do not require a live model
    vllm_contract: opt-in tests against the pinned live vLLM/model/tokenizer matrix
